<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.birzeit.assigment2">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Assigment2">
        <activity android:name=".activiteis.EditActivity2"></activity>
        <activity android:name=".activiteis.EditActivity" />
        <activity android:name=".activiteis.PreviewActivity" />
        <activity android:name=".activiteis.ContactActivity" />
        <activity android:name=".activiteis.WorkActivity" />
        <activity android:name=".activiteis.EducationActivity" />
        <activity android:name=".activiteis.PersonalInfoActivity" />
        <activity android:name=".activiteis.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>